spring:
  profiles:
    active: local
  application:
    name: tron-worker
#  cloud:
#    config:
#      uri: http://config:8888
#      fail-fast: true
#      password: ${CONFIG_SERVICE_PASSWORD}
#      username: user
server:
  port: 80
